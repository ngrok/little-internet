# tsharkie validation — 2026-09-09

User requested a named wrapper for the live save/format/pager pipeline with a
pcapng filename and capture filter. Source is tools/tsharkie/tsharkie, installed
at /usr/local/bin/tsharkie on pi-foo-01, pi-foo-02, and pi-foo-dhcp. Each install
reported that executable path and its expected help usage. No network profiles
were changed, and no image was rebuilt.

The initial Mac loopback check failed with a BPF permission error. It produced
no live packet evidence. The successful check used pi-foo-01's loopback (`lo`),
`-f icmp`, `--no-pager`, and ping to 127.0.0.1. This did not send packets over
the lesson Ethernet network. Actual contiguous output excerpt:

```text
Live rows before stopping: True
 No.  Time(s)  Source             Destination        Proto  Info
   1    0.000  127.0.0.1          127.0.0.1          ICMP   Echo (ping) request  id=0x000a, seq=1/256, ttl=64
```

The test then interrupted the capture process group; a partial second formatted
row was omitted from the excerpt above. The pcapng was read back successfully:

```text
Capture path: /tmp/tsharkie-check-8nfehfif/live capture.pcapng
Saved packets: 2
Existing capture refusal: tsharkie: capture already exists: /tmp/tsharkie-check-8nfehfif/live capture.pcapng
Argument validation passed.
```

Verified a capture path containing spaces, live output before process exit,
readable saved packets, overwrite refusal, and errors for missing filename,
missing filter value, unknown option, and multiple filenames. `bash -n` and
`git diff --check` passed locally. ShellCheck is not installed. The interactive
less view was not automatically exercised; the underlying viewer command is
the previously supplied `less -S +F`. DHCP exchanges remain part of the actual
build/rehearsal, not this wrapper test.

Earlier input-buffering check on pi-foo-01 identified /usr/bin/awk as mawk
1.3.4 20200120. Actual synthetic-input results:

```text
default mawk: first output at 2.76s; process ended at 2.76s; output='synthetic test row'
interactive mawk: first output at 0.75s; process ended at 2.78s; output='synthetic test row'
```

The wrapper uses `mawk -W interactive` when available and system awk otherwise.
